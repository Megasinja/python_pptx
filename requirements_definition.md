# 研修用 Python 実行環境 要件定義（v0.1）

更新日: 2026-02-27

## 1. 目的
- 研修で「説明資料を見ながら、その場で Python を実行・確認」できる Web 環境を提供する。
- 画面は **左側: Python 実行環境**、**右側: Canvas（資料表示エリア）** の 2 ペインを基本とする。
- 講師・受講者がストレスなく使える操作性と、同時利用に耐える安定性を確保する。

## 2. 想定ユーザー
- 講師: 資料表示、サンプルコード提示、進行管理
- 受講者: コード編集、実行、結果確認、資料参照
- 運用管理者: コース/資料管理、利用監視

## 3. スコープ
### MVP（初期リリース）
- 左ペインで Python コード編集・実行・標準出力表示
- 右ペインで資料（PDF/画像）表示、ページ送り
- WebSocket による実行ログのリアルタイム配信
- ユーザーごとの実行セッション分離
- 実行時間・メモリの制限

### 将来拡張
- Canvas への書き込み（注釈、図形）
- 講師画面との同期（講師が開いたページへ追従）
- 共同編集、提出機能、採点連携

## 4. 機能要件
### 4.1 画面要件
- 2 カラム固定レイアウト（可変幅リサイズ可）
- 左ペイン:
  - コードエディタ（シンタックスハイライト、行番号）
  - 実行ボタン、停止ボタン、初期化ボタン
  - 実行結果コンソール（stdout/stderr）
- 右ペイン:
  - Canvas 領域に資料を表示
  - ページ送り/戻し、ズーム、フィット表示

### 4.2 実行要件
- Python 3.x を実行
- 実行はユーザー単位の分離環境（プロセスまたはコンテナ）
- タイムアウト（例: 10 秒）と強制停止
- 標準出力を逐次ストリーミング表示

### 4.3 セッション要件
- ログイン後にセッション生成
- ブラウザ再接続時のセッション復元（一定時間）
- 同一ユーザー多重起動の制御（ポリシーで選択）

### 4.4 資料管理要件
- 講座単位で資料を登録
- 資料形式: PDF, PNG, JPG（MVP）
- 右ペインに教材を即時反映

## 5. 非機能要件
- 性能:
  - 実行開始までの遅延: 1 秒以内（通常時）
  - コンソール反映遅延: 300ms 以内
- 同時利用:
  - 1 クラス 30〜50 名を想定（将来 100 名）
- 可用性:
  - 研修時間帯の安定稼働を優先
- セキュリティ:
  - 任意コード実行に対するサンドボックス必須
  - ネットワーク/ファイルアクセス制御
- 監査性:
  - 実行ログ、エラーログ、操作ログを保持

## 6. 技術要件（推奨）
「WebSocket とコンカレンシを使っていた」という条件を前提に、以下を推奨。

### 6.1 フロントエンド
- React + TypeScript
- コードエディタ: Monaco Editor
- コンソール表示: xterm.js
- 右ペイン描画:
  - PDF 表示: pdf.js
  - Canvas 操作: Konva.js または Fabric.js

### 6.2 バックエンド
- FastAPI（Python）+ Uvicorn
- WebSocket エンドポイントで双方向通信
- 非同期実装: asyncio
- 同時実行制御:
  - 軽量: asyncio + subprocess
  - 本格: ワーカー（Celery/RQ）+ Redis

### 6.3 実行基盤
- Docker でユーザー実行環境を隔離
- リソース制限（CPU/メモリ/実行時間）
- 1 ユーザー 1 実行コンテキスト（またはジョブ単位コンテナ）

### 6.4 データ基盤
- PostgreSQL: ユーザー、講座、資料メタ情報
- Redis: セッション、キュー、WebSocket 接続状態
- オブジェクトストレージ（S3/互換）: 教材ファイル

## 7. WebSocket イベント設計（例）
- `session.init`: セッション開始
- `code.run`: 実行要求
- `code.stdout`: 標準出力ストリーム
- `code.stderr`: 標準エラーストリーム
- `code.done`: 実行終了
- `code.abort`: 実行停止
- `canvas.page.change`: 右ペイン資料ページ変更
- `heartbeat`: 接続維持

## 8. コンカレンシ設計の要点
- WebSocket 接続ごとに非同期タスクを管理
- 実行ジョブはメイン API スレッドから分離（ブロッキング回避）
- ジョブキューで同時実行数を制限し、過負荷時は待機
- キャンセル要求時にジョブ/プロセスを中断できる設計

## 9. 受け入れ基準（MVP）
- 左ペインで Python コードが実行でき、結果が逐次表示される
- 右ペインで資料を表示し、ページ操作できる
- 30 名同時接続で重大エラーなく 1 時間運用できる
- 1 ユーザーの暴走コードが他ユーザーへ影響しない

## 10. 未確定事項（次ステップで要決定）
- 講師主導同期（受講者画面を追従させるか）
- 実行環境の永続化範囲（セッション終了後に破棄するか）
- 外部ライブラリの許可方針（pip install 可否）
- 認証方式（社内 SSO / ID・PW）
